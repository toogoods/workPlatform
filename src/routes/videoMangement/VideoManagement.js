import React from 'react'
import { connect } from 'dva';

const VideoManagement = () => {
    return (<div>
        asdf
    </div>)
}

export default connect(({video}) => ({video}))(VideoManagement);